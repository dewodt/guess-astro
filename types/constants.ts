import { modes, matchResults } from "@/lib/constants";

// Modes type
export type ModesType = (typeof modes)[number];

// Match result type
export type MatchResultsType = (typeof matchResults)[number];
