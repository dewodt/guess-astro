describe("Leaderboard Mode Page", () => {
  // Mock db
  // For each modes it should
  it.skip("Should render the leaderboard page successfully", () => {});

  it.skip("Should render the correct leaderboard data");

  it.skip("Should render modal and intercept route successfully when table row is clicked", () => {});
});
