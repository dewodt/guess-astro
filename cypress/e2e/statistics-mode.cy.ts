describe("Statistics Mode Page", () => {
  // Mock db
  // For each modes it should
  it.skip("Should redirect to login page when user is not authenticated", () => {});

  it.skip("Should render the statistics page successfully", () => {});

  it.skip("Should render the correct statistics data for a user", () => {});
});
