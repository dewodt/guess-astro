describe("Play Mode Page", () => {
  // Mock db and auth
  // For each modes it should
  it.skip("Should redirect to login page when user is not authenticated", () => {});

  it.skip("Should render the play mode page successfully", () => {});

  it.skip("Should be able to submit answer successfully", () => {});
});
